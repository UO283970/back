name: Backend

on: [push]

jobs:
  example-job:
    runs-on: ubuntu-latest
    steps:
    - shell: bash
      env:
        SECRET_PASS_DECRYPTION_KEY: ${{ secrets.SECRET_PASS_DECRYPTION_KEY }}
      run: |
        example-command "$SECRET_PASS_DECRYPTION_KEY"
